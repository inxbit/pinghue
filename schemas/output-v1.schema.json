{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/inxbit/pinghue/main/schemas/output-v1.schema.json",
  "title": "pinghue output v1",
  "description": "Output document produced by pinghue --output PATH. One file per run. Existing regular files are preserved unless --overwrite is passed. Breaking schema changes increment schema_version. Per-target `stats` are computed over every probe sent during the run (`stats.sent`); the `samples` array retains only the most recent `run.samples_window` samples per target, so `samples` can be shorter than `stats.sent` on long runs. `--no-samples` emits empty `samples` arrays.",
  "type": "object",
  "required": ["schema_version", "pinghue_version", "run", "targets"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "const": 1,
      "description": "Major schema version. Consumers must check this value."
    },
    "pinghue_version": {
      "type": "string"
    },
    "run": {
      "$ref": "#/$defs/run"
    },
    "targets": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/target"
      }
    }
  },
  "$defs": {
    "run": {
      "type": "object",
      "required": ["started_at", "ended_at", "host", "exit_reason", "samples_window", "probe"],
      "additionalProperties": false,
      "properties": {
        "started_at": {
          "type": "string",
          "format": "date-time"
        },
        "ended_at": {
          "type": "string",
          "format": "date-time"
        },
        "host": {
          "type": "string"
        },
        "exit_reason": {
          "type": "string",
          "enum": ["user_quit", "interrupted", "deadline", "completed", "error"]
        },
        "samples_window": {
          "type": "integer",
          "minimum": 0,
          "description": "Maximum number of most-recent probe samples retained per target in its samples array. This value is reduced uniformly for large target sets to enforce the run-wide retention budget. Per-target stats are computed over all probes (stats.sent), which can exceed this value on long runs."
        },
        "probe": {
          "$ref": "#/$defs/probe_config"
        }
      }
    },
    "probe_config": {
      "type": "object",
      "required": ["mode", "port", "interval_s", "timeout_s", "address_family"],
      "additionalProperties": false,
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["icmp", "tcp"]
        },
        "port": {
          "type": ["integer", "null"],
          "minimum": 1,
          "maximum": 65535
        },
        "interval_s": {
          "type": "number",
          "minimum": 0.1
        },
        "timeout_s": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "address_family": {
          "type": "string",
          "enum": ["auto", "ipv4", "ipv6"]
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "mode": {
                "const": "tcp"
              }
            }
          },
          "then": {
            "properties": {
              "port": {
                "type": "integer"
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "mode": {
                "const": "icmp"
              }
            }
          },
          "then": {
            "properties": {
              "port": {
                "type": "null"
              }
            }
          }
        }
      ]
    },
    "target": {
      "type": "object",
      "required": ["target", "resolved_address", "resolved_family", "status", "error", "stats", "samples"],
      "additionalProperties": false,
      "properties": {
        "target": {
          "type": "string"
        },
        "resolved_address": {
          "type": ["string", "null"]
        },
        "resolved_family": {
          "type": ["string", "null"],
          "enum": ["ipv4", "ipv6", null]
        },
        "status": {
          "type": "string",
          "enum": ["resolving", "healthy", "intermittent", "down", "dns_failure", "permission_denied", "error"]
        },
        "error": {
          "type": ["string", "null"]
        },
        "stats": {
          "$ref": "#/$defs/stats"
        },
        "samples": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/sample"
          }
        }
      }
    },
    "stats": {
      "type": "object",
      "required": ["sent", "received", "loss_pct", "min_ms", "avg_ms", "max_ms", "jitter_ms"],
      "additionalProperties": false,
      "properties": {
        "sent": {
          "type": "integer",
          "minimum": 0
        },
        "received": {
          "type": "integer",
          "minimum": 0
        },
        "loss_pct": {
          "type": "number",
          "minimum": 0,
          "maximum": 100
        },
        "min_ms": {
          "type": ["number", "null"],
          "minimum": 0
        },
        "avg_ms": {
          "type": ["number", "null"],
          "minimum": 0
        },
        "max_ms": {
          "type": ["number", "null"],
          "minimum": 0
        },
        "jitter_ms": {
          "type": ["number", "null"],
          "minimum": 0,
          "description": "RFC 3550 interarrival jitter in milliseconds: the smoothed mean absolute difference between consecutive latencies, not the latency standard deviation."
        }
      }
    },
    "sample": {
      "type": "object",
      "required": ["timestamp", "latency_ms", "status", "error"],
      "additionalProperties": false,
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time"
        },
        "latency_ms": {
          "type": ["number", "null"],
          "minimum": 0
        },
        "status": {
          "type": "string",
          "enum": ["ok", "timeout", "unreachable", "refused", "error"]
        },
        "error": {
          "type": ["string", "null"]
        }
      }
    }
  }
}
